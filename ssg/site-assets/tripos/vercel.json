{
  "buildCommand": "cd ../../ssg && python3 -m pip install -r requirements.txt && python3 run.py tripos && cd ../deployments/tripos && npm run build",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- ./ssg ./sources/tripos || exit 1",
  "git": {
    "deploymentEnabled": {
      "dev": false
    }
  }
}