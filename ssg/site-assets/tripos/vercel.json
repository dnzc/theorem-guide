{
  "buildCommand": "cd ../../ssg && python3 -m pip install -r requirements.txt && python3 run.py tripos && cd ../deployments/tripos && npm install && npm run build",
  "git": {
    "deploymentEnabled": {
      "dev": false
    }
  }
}