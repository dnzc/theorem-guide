{
  "buildCommand": "cd ../../ssg && python3 -m pip install -r requirements.txt && python3 run.py danielc && cd ../deployments/danielc && npm install && npm run build",
  "git": {
    "deploymentEnabled": {
      "dev": false
    }
  }
}